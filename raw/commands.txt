sudo su           # Switch to root
yum update -y     # Update all packages
reboot            # Reboot if kernel or critical system libraries

gcc --version         # C compiler
g++ --version         # C++ compiler
python3 --version     # Python
git --version         # Git

yum groupinstall "Development Tools" -y      # Installs gcc, g++, make, etc.
yum install git python3 -y                   # Installs Git and Python 3

mkdir folder_name      # Create a folder
rmdir folder_name      # Remove a folder
rm -rf folder_name     # Remove a non-empty folder

touch file_name.ext     # Create a file
nano file_name.ext      # Create and write in the file
rm file_name.ext        # Remove a file

git clone https://github.com/niranjan-bhol/aws_ec2_alpha.git temp-repo && mv temp-repo/* temp-repo/.* . 2>/dev/null && rm -rf temp-repo     # Clone all the folders, files present in the github repo, without cloning parent directory

sudo yum install python3-pip -y     # Install pip3

pip3 install requests pyotp yfinance yahooquery kiteconnect numpy pandas scikit-learn tabulate asyncio aiohttp datetime time

timedatectl     # Check current time settings

sudo timedatectl set-timezone Asia/Kolkata     # Set timezone to Asia/Kolkata

ping kite.trade
ping 104.16.34.50
ping -c 10 kite.trade
ping -c 10 104.16.34.50
dig+ short kite.trade 

sudo yum install cronie -y     # Install the cron service

sudo systemctl start crond
sudo systemctl enable crond     # Start and enable cron

systemctl status crond     # Edit your cron jobs

which python3     # Check if it’s running

sudo python3 -m pip install kiteconnect       # Install kiteconnect library for root
python3 -m pip install --user kiteconnect     # Install kiteconnect library for ec2-user

sudo crontab -e     # Edit your cron jobs

10 9 * * * /usr/bin/python3 /path/to/login.py >> /path/to/login.log 2>&1                             # to run login.py at 9:10 AM daily
50 8 * * * /usr/bin/python3 /home/ec2-user/alpha/login.py >> /home/ec2-user/alpha/login.log 2>&1     # to run login.py at 8:50 AM daily

